apply plugin: 'com.android.library'

android {
    compileSdk rootProject.ext.compileSdkVersion
	defaultConfig {
		minSdkVersion 26
	}
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
    namespace 'com.mpatrick.mp3agic'
}
